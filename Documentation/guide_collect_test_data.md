---
title: "guide_collect_test_data"
output: html_document
---

Отлично! Теперь у меня есть полная картина. Вот инструкция по использованию тестов для `collect_data.R` на 300+ строк:

```markdown
# Руководство по тестированию collect_data.R

## Оглавление

1. [Введение](#введение)
2. [Структура тестов](#структура-тестов)
3. [Подготовка к запуску](#подготовка-к-запуску)
4. [Запуск тестов](#запуск-тестов)
5. [Описание тестовых блоков](#описание-тестовых-блоков)
6. [Интерпретация результатов](#интерпретация-результатов)
7. [Решение проблем](#решение-проблем)
8. [Расширение тестов](#расширение-тестов)

---

## Введение

Модуль `collect_data.R` является ключевым компонентом для сбора данных из arXiv API. Тестовый файл `test_collect_data.R` содержит 347 строк комплексных тестов, покрывающих все аспекты работы модуля сбора данных.

### Что тестируется

- Корректность структуры данных
- Валидация входных параметров
- Фильтрация по кибербезопасности
- Сохранение и загрузка данных
- Вспомогательные функции
- Интеграционные сценарии
- Производительность
- Обработка краевых случаев

---

## Структура тестов

Тесты организованы в 7 основных блоков:

```
tests/manual/test_collect_data.R
├── 1. Тесты fetch_arxiv_data()
├── 2. Тесты filter_cybersecurity()
├── 3. Тесты save_collected_data()
├── 4. Тесты вспомогательных функций
├── 5. Интеграционные тесты
├── 6. Тесты производительности
└── 7. Тесты краевых случаев
```

### Зависимости

Тесты требуют следующие пакеты:

```
library(testthat)      # Фреймворк тестирования
library(httr)          # HTTP запросы
library(xml2)          # Парсинг XML
library(dplyr)         # Манипуляции с данными
library(stringr)       # Работа со строками
library(tibble)        # Современные датафреймы
library(lubridate)     # Работа с датами
library(purrr)         # Функциональное программирование
```

---

## Подготовка к запуску

### Шаг 1: Установка зависимостей

```
# Установка пакетов (если отсутствуют)
install.packages(c(
  "testthat", "httr", "xml2", "dplyr", 
  "stringr", "tibble", "lubridate", "purrr"
))
```

### Шаг 2: Настройка путей

Откройте файл `test_collect_data.R` и измените путь к модулю:

```
# ВАЖНО: Измените путь на свой
source("~/Desktop/arxiv-cyber-feed/R/collect_data.R")

# Например:
# source("C:/Users/YourName/arxiv-cyber-feed/R/collect_data.R")  # Windows
# source("/home/user/arxiv-cyber-feed/R/collect_data.R")         # Linux
```

### Шаг 3: Проверка подключения к интернету

Большинство тестов требуют подключения к arXiv API:

```
# Проверить доступность API
httr::GET("http://export.arxiv.org")
```

---

## Запуск тестов

### Вариант 1: Запуск всех тестов (рекомендуется)

```
# В консоли R
testthat::test_file("tests/manual/test_collect_data.R")
```

**Время выполнения:** ~2-5 минут (в зависимости от интернета)

### Вариант 2: Запуск в RStudio

1. Откройте файл `test_collect_data.R` в RStudio
2. Нажмите **Ctrl+Shift+T** (запуск всех тестов в файле)
3. Или нажмите **Ctrl+Enter** для поэтапного выполнения

### Вариант 3: Запуск отдельных блоков

```
# Загрузить библиотеки и модуль
source("tests/manual/test_collect_data.R", echo=FALSE, max.deparse.length=0)

# Запустить только тесты fetch_arxiv_data()
testthat::test_that("fetch_arxiv_data возвращает корректную структуру", {
  # код теста
})
```

### Вариант 4: Запуск через командную строку

```
# В терминале (macOS/Linux)
Rscript -e "testthat::test_file('tests/manual/test_collect_data.R')"

# Windows PowerShell
Rscript.exe -e "testthat::test_file('tests/manual/test_collect_data.R')"
```

---

## Описание тестовых блоков

### Блок 1: Тесты fetch_arxiv_data()

**Цель:** Проверить корректность основной функции сбора данных.

#### Тест 1.1: Корректная структура данных

```
test_that("fetch_arxiv_data возвращает корректную структуру", {
  test_data <- fetch_arxiv_data(max_results = 2, verbose = FALSE)
  
  # Проверяет:
  # - Тип возвращаемого объекта (tibble)
  # - Наличие всех обязательных колонок
  # - Порядок колонок
})
```

**Что проверяется:**
- Возвращается tibble (современный датафрейм)
- Есть 8 обязательных колонок: arxiv_id, title, authors, abstract, categories, published_date, doi, collection_date
- Колонки не переименованы

#### Тест 1.2: Валидация параметров

```
test_that("fetch_arxiv_data валидирует параметры", {
  # Проверяет 5 сценариев:
  # 1. max_results = 0 → ошибка
  # 2. max_results = 2000 → ошибка
  # 3. Невалидные категории → предупреждение
  # 4. Только невалидные категории → ошибка
  # 5. Пустой вектор категорий → ошибка
})
```

**Ожидаемое поведение:**
- Диапазон `max_results`: 1-1000
- Допустимые категории: cs.CR, cs.AI, cs.NI, cs.SE, cs.DC, cs.LG, cs.CY, cs.DB, cs.IR, stat.ML, math.OC

#### Тест 1.3: Работа с несколькими категориями

```
test_that("fetch_arxiv_data работает с несколькими категориями", {
  data <- fetch_arxiv_data(
    categories = c("cs.CR", "cs.AI"),
    max_results = 3,
    verbose = FALSE
  )
  
  # Проверяет типы данных для каждой колонки
})
```

---

### Блок 2: Тесты filter_cybersecurity()

**Цель:** Проверить фильтрацию публикаций по кибербезопасности.

#### Тест 2.1: Корректная фильтрация

```
test_that("filter_cybersecurity корректно фильтрует данные", {
  # Создает 3 тестовые статьи:
  # - 2 по безопасности (с ключевыми словами "security", "cryptography")
  # - 1 не по безопасности (machine learning)
  
  # Ожидается: 2 статьи из 3
})
```

**Проверяемые ключевые слова:**
- security, cybersecurity, threat, attack, malware
- cryptography, encryption, cipher
- firewall, intrusion, authentication
- privacy, anonymization, data protection

#### Тест 2.2: Обработка пустых данных

```
test_that("filter_cybersecurity обрабатывает пустые данные", {
  # Передается пустой tibble
  # Ожидается: предупреждение + пустой tibble
})
```

#### Тест 2.3: Строгий режим

```
test_that("filter_cybersecurity работает в строгом режиме", {
  # strict_mode = TRUE добавляет расширенные ключевые слова:
  # - threat intelligence, APT, MITRE ATT&CK
  # - incident response, digital forensics
  # - SOC, SIEM, SOAR, EDR, XDR
})
```

---

### Блок 3: Тесты save_collected_data()

**Цель:** Проверить сохранение и загрузку данных.

#### Тест 3.1: Сохранение и загрузка

```
test_that("save_collected_data сохраняет и загружает данные", {
  # 1. Создает тестовые данные
  # 2. Сохраняет в временный файл
  # 3. Проверяет существование файла
  # 4. Загружает обратно
  # 5. Сравнивает данные
  # 6. Удаляет временный файл
})
```

#### Тест 3.2: Создание директорий

```
test_that("save_collected_data создает директории", {
  # Путь: /temp/test/nested/data.rds
  # Проверяет автоматическое создание вложенных директорий
})
```

#### Тест 3.3: Обработка ошибок

```
test_that("save_collected_data обрабатывает ошибки", {
  # Сценарий 1: Пустые данные → предупреждение
  # Сценарий 2: Пустой путь → ошибка
})
```

---

### Блок 4: Тесты вспомогательных функций

#### Тест 4.1: Построение запросов

```
test_that(".construct_arxiv_query формирует корректные запросы", {
  # Одна категория: "cat:cs.CR"
  # Две категории: "(cat:cs.CR OR cat:cs.AI)"
  # Три категории: "(cat:cs.CR OR cat:cs.AI OR cat:cs.NI)"
})
```

#### Тест 4.2: Парсинг дат

```
test_that(".parse_datetime корректно парсит даты", {
  # Валидная дата: "2024-01-15T12:30:45Z" → POSIXct
  # Невалидная дата: "not-a-date" → NA
})
```

---

### Блок 5: Интеграционные тесты

**Цель:** Проверить полный цикл ETL (Extract-Transform-Load).

```
test_that("Полный ETL цикл работает корректно", {
  # 1. Fetch: Получить данные из arXiv
  # 2. Filter: Отфильтровать по кибербезопасности
  # 3. Save: Сохранить в файл
  # 4. Load: Загрузить обратно
  # 5. Compare: Сравнить данные
})
```

**Проверяемая цепочка:**
```
arXiv API → fetch_arxiv_data() → filter_cybersecurity() → 
save_collected_data() → readRDS()
```

---

### Блок 6: Тесты производительности

```
test_that("Операции выполняются за разумное время", {
  # fetch_arxiv_data(2) должен занимать < 10 секунд
  # filter_cybersecurity(5) должен занимать < 2 секунд
})
```

**Бенчмарки:**
- Сбор 2 статей: < 10 сек
- Фильтрация 5 статей: < 2 сек

---

### Блок 7: Тесты краевых случаев

```
test_that("Обработка краевых случаев", {
  # Минимальные данные: max_results = 1
  # Большие данные: max_results = 100 (только в интерактивном режиме)
})
```

---

## Интерпретация результатов

### Успешное выполнение

```
Test passed 🌈
✓ | 24 passed | 0 warnings | 0 errors
```

Все тесты прошли успешно.

### Частичный успех

```
Test passed 😀
✓ | 20 passed | 3 skipped | 0 warnings | 0 errors
```

Некоторые тесты пропущены (обычно из-за отсутствия интернета).

### Ошибки

```
Test failed 😞
✓ | 15 passed | 0 skipped | 2 warnings | 3 errors
```

Есть ошибки — см. детали ниже.

---

## Решение проблем

### Проблема 1: "could not find function 'fetch_arxiv_data'"

**Причина:** Не загружен модуль `collect_data.R`.

**Решение:**
```
source("R/collect_data.R")
```

### Проблема 2: "Error: package 'testthat' not found"

**Причина:** Не установлен пакет testthat.

**Решение:**
```
install.packages("testthat")
```

### Проблема 3: Все тесты пропускаются (skipped)

**Причина:** Нет подключения к интернету или arXiv API недоступен.

**Решение:**
```
# Проверить подключение
httr::GET("http://export.arxiv.org")

# Если ошибка 403/503 — подождите несколько минут
```

### Проблема 4: Тесты выполняются слишком долго

**Причина:** Мед